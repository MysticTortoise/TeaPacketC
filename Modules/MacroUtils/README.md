# MacroUtils Module
This Interface-Only Module contains many useful macros across all platforms.

The most useful file is Features.h, which contains feature macros that check support for different C features.
If those features are unavailable, it may provide a substitute where applicable.