This implementation of the Assets module uses the standard `std::fstream` library of C++ for file i/o,
and reads from an "assets" folder placed in the current working directory.

It should compile on any platform with support for `std::filesystem.`