This implementation of the Input Module uses the Windows GameInput library as a backend.


## Requirements
Compiling this will attempt to install GameInput, but doing so requires `vcpkg` is installed.