This implementation of the System module handles processing for a Windows application.

It depends on the Windows Module Implementation of the Window module. That module must be used for this one to work.