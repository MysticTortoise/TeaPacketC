# Window Module
This module handles multiple Windows on Desktop based platforms.
It does NOT handle Graphics, but it does handle window creation and interaction.

This Module is the only way to create new Displays at runtime.