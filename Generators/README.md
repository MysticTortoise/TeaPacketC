# Generators
Generators are used in TeaPacket for code generation. Any non-specific code generators should be placed here.

The standard way of accessing these are through a Functions.CMake file placed inside their directory. 
Calling the functions defined in there should be enough to generate code through these.

See each Generator's README.md file for info on what each one does.